# PostgreSQL（外部实例，必填）
DB_HOST=your-postgres-host
DB_PORT=5432
DB_NAME=algo
DB_USER=algo
DB_PASSWORD=your-password

# TDengine REST API（外部实例，必填）
TD_HOST=your-tdengine-host
TD_PORT=6041
TD_USER=root
TD_PASSWORD=taosdata
TD_DATABASE=hiccpet_device
TD_SUPERTABLE=imu_data

# 服务配置（可选，默认值已够用）
LOG_LEVEL=info
FETCH_INTERVAL_MIN=15
